package org.app.custom.errorlist;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Sample Springboot application that validates and returns custom error list
 */
@SpringBootApplication
public class SampleErrorlistApplication {
	public static void main(String[] args) {
		SpringApplication.run(SampleErrorlistApplication.class, args);
	}
}
